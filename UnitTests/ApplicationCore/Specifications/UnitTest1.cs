using System;
using System.Linq;
using Ardalis.Specification.EntityFrameworkCore;
using Airlines.ApplicationCore.Entities;
using Airlines.ApplicationCore.Specifications;
using Xunit;

namespace UnitTests.ApplicationCore.Specifications
{
    public class FindFlightsSpecificationTests
    {
        /*private readonly int _testBasketId = 123;

        private readonly SpecificationEvaluator<FlightInstance> _evaluator = new SpecificationEvaluator<FlightInstance>();
        
        [Fact]
        public void MatchesNoBasketsIfBasketIdNotPresent()
        {
            int badBasketId = -1;
            var spec = new FindFlightsSpecification();

            var result = _evaluator.GetQuery(GetTestBasketCollection().AsQueryable(), spec)
                .Any();

            Assert.False(result);
        }*/
    }
}